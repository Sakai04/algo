#include <iostream>
using namespace std;

int main(){
    ios::sync_with_stdio(0);
    cin.tie(0);

  int N, V, arr[201] = {};
  cin >> N;

  while (N--)
  {
    int X;
    cin >> X;
    arr[X + 100];
  }

  cin >> V;
  cout << arr[V + 100];

}